using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;
using aspnetCoreReactTemplate.Models;

namespace aspnetCoreReactTemplate
{
  // public static class PagedListExtensions {
  //   public static PagedList<T> toPagedList<T>(this IQueryable<T> ob, PagedListOptions options) {
  //     ob.OrderBy(options.SortProperty + (options.Decending ? " desc" : ""));
  //     ob.Skip(((options.PageNumber - 1) * options.PageSize));
  //     ob.Take(options.PageSize);
  //     return (PagedList<T>) ob.ToList();
  //   }
  // }
}
